<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:orientation="vertical"
        android:background="?selectableItemBackground"
        android:layout_width="match_parent"
        android:clickable="true"
        android:focusable="true"
        android:layout_height="64dp"
        android:paddingStart="@dimen/item_backend_padding"
        android:paddingEnd="@dimen/item_backend_padding"
        android:id="@+id/relativeLayout">

    <TextView
            android:id="@+id/itemAdminTextviewName"
            tools:text="Apple iPhone XS 256Gb Rose Gold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.AppCompat.Body2"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginBottom="4dp"
            app:layout_constraintBottom_toTopOf="@+id/itemAdminTextviewPrice"
            app:layout_constraintEnd_toStartOf="@+id/itemAdminTextviewQuantity"
            app:layout_constraintHorizontal_chainStyle="spread_inside"
            app:layout_constraintVertical_chainStyle="packed"
            app:layout_constraintHorizontal_bias="1.0"/>

    <TextView
            android:id="@+id/itemAdminTextviewQuantity"
            tools:text="4"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:textAppearance="@style/TextAppearance.AppCompat.Body2"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/itemAdminTextviewPrice"
            app:layout_constraintTop_toTopOf="@+id/itemAdminTextviewName"
            app:layout_constraintStart_toEndOf="@+id/itemAdminTextviewName"
            android:layout_marginStart="8dp"
            app:layout_constraintHorizontal_bias="1.0"/>

    <TextView
            android:id="@+id/itemAdminTextviewPrice"
            tools:text="100000"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/itemAdminTextviewName"
            tools:layout_conversion_wrapWidth="126"
            tools:layout_conversion_wrapHeight="50"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginEnd="8dp"
            app:layout_constraintEnd_toEndOf="@+id/itemAdminTextviewName"
            app:layout_constraintHorizontal_bias="0.0"/>


</androidx.constraintlayout.widget.ConstraintLayout>